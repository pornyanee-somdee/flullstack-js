function findPerimeter(length, width){
    const performance = 2* (length + width);
    return performance;
}
//let result = findPerimeter(5,10); 
console.log(`the perimeter id: ${findPerimeter(5,10)}`); // result